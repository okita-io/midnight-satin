/**
 * Clerk visual theming is owned by the Clerk Dashboard (Production/Development
 * → Customization → Theme). Do not set `appearance` on ClerkProvider / SignIn /
 * SignUp here — code overrides fight Dashboard themes.
 *
 * Keep `/sign-in` and `/sign-up` routes for `auth.protect()` redirects; interactive
 * CTAs should use `<SignInButton mode="modal" />` / `<SignUpButton mode="modal" />`.
 */

export {};
